/**
 * Couche de présentation REST (Contrôleurs).
 * Expose les endpoints pour l'authentification, la gestion des serveurs FTP,
 * ainsi que les opérations sur les fichiers et répertoires.
 */
package univ.sr2.flopbox.controller;